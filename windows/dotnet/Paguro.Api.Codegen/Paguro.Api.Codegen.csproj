<Project Sdk="Microsoft.NET.Sdk">
  <!-- Generates the C# API types from windows/api/paguro-api.json; run by
       Paguro.Api's build, so there is no hand-maintained copy. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <RollForward>Major</RollForward>
  </PropertyGroup>
</Project>
