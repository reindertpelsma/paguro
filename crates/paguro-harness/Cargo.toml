[package]
name = "paguro-harness"
license.workspace = true
description = "The storage torture harness — the first artifact to build (DESIGN.md §1b, §11)"
edition.workspace = true
version.workspace = true
publish.workspace = true
rust-version.workspace = true
# src/bin holds tools (paguro-bde-write); CI runs `cargo run -p paguro-harness`.
default-run = "paguro-harness"

[dependencies]
libc = "0.2"
paguro-core.workspace = true
paguro-crypto.workspace = true
paguro-boot.workspace = true
sha2.workspace = true

[build-dependencies]
cc = "1"

[lints]
workspace = true
